using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*public class moveMayo19 : MonoBehaviour
{
    Rigidbody Rb;
    float inputx;
    public float speed; 
    float inputz;
    public Vector3 inputDirection;
    Vector3 rotationSpeed = new Vector3(0, 100, 0);
    Animator AnimatorController;




   // public float vida = 10.0f;
   // public float Disparo = 20.0f;
   // public float speed = 5.0f;


   // static moveMayo19 instatiablePlayer;
   // private void Awake()
   // {
   //     instatiablePlayer = this;
   // }

 //   public static moveMayo19 getInstance()
  //  {
  //      return instatiablePlayer;
   // }

    // Start is called before the first frame update
    void Start()
    {
        Rb = GetComponent<Rigidbody>();
    }

    // Update is called once per frame
    private void Update()
    {
        movimiento();
        AnimatorController.SetFloat("avanzar", inputDirection.z);
    }


   // void movimiento()
   // {
     //   Vector3 m_input = new Vector3(Input.GetAxisRaw("Horizontal").  0,Input.GetAxisRaw("vertical"));
     //   inputDirection = m_input.normalized;

    //    Rb.MovePosition(Rb.position + (transform.avanzar = inputDirection.z * Time.deltaTime * speed));
    //    Quaternion deltaRotation = Quaternion.Euler(inputDirection.x * rotationSpeed * Time.deltaTime);
   // }
//}*/
