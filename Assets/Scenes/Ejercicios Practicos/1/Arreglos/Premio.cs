using System.Collections;
using System.Collections.Generic;
using UnityEngine;


public class Premio

    { 

    public string nombre;
    public int premios;

    public Premio(string newnombre, int newpremios)

    {
        nombre = newnombre;
        premios = newpremios; 
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
