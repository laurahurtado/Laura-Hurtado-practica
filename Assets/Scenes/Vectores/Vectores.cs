using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*public class Vectores : MonoBehaviour
{
    public float Velocidad = 5;
    public GameObject Enemy;

    void Start()
    {
        Vector2 posiEnemy = Enemy.transform.position;
        Vector2 posiplayer = transform.position;

        Vector2 distancia = posiplayer - posiEnemy;
        print(distancia.magnitude);


        float distanciaUnity = Vector2.Distance(posiEnemy, posiplayer);
        print(distanciaUnity);

        if (distanciaUnity < 5) ;
        {

        }
    }

    // Update is called once per frame
    void Update()
    {
        

    }

   // private void OnDrawGizmos()
    {
        Vector2 posiEnemy = Enemy.transform.position;
        Vector2 posiplayer = transform.position;

        float dotVariable = Vector2.Dot(posiEnemy, posiplayer);


        Gizmos.color = Color.green;
        Gizmos.DrawLine(puntoCero, posiplayer);

        Gizmos.color = Color.red;
        Gizmos.DrawLine(puntoCero, posiEnemy);

        Vector3 crossProduct = Vector3.Cross(posiEnemy, posiplayer);

        Gizmos.color = Color.blue;
        Gizmos.DrawLine(puntoCero, crossProduct);//

    }
}*/

